RewriteEngine On

# Keep real files/folders in public_html working (subdomains, logos, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Forward all rankxseo.com requests to the deployed app folder
RewriteRule ^(.*)$ rankx/public/$1 [L,QSA]
